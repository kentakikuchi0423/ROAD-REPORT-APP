# プロジェクト概要

このプロジェクトは、**愛媛県大洲市の道路の破損通報を受け付けるアプリ**を実装するためのものです。

参考にするのは、**愛媛県四国中央市の道路破損通報の導線や考え方**です。
ただし、実際に対象とする自治体・道路は **大洲市** です。

# アプリの目的

市民が LINE 上で道路の破損を通報できるようにする。
ユーザーは LINE チャット上で対話しながら必要情報を送信し、通報完了後に受付番号を受け取れるようにする。
管理者は Web 管理画面から通報内容を確認・更新・出力できるようにする。

# チャネル・UX方針

- LINEチャット上で完結する
- LIFF前提ではない
- 位置情報は LINE 標準の位置情報送信を利用する

# 入力要件

## 必須
- 近景写真1枚
- 遠景写真1枚
- 位置情報

## 任意
- 撮影日付
- 補足事項
- 氏名
- 電話番号

## 通報完了時
- 受付番号を返す

## 匿名性
- 匿名通報可
- 氏名・電話番号未入力でも完了可能

# 管理画面要件

- 一覧
- 詳細
- ステータス変更
- CSV出力
- 写真ダウンロード
- 削除

# 技術方針

- TypeScript を基本とする
- Cloudflare Workers / Pages / D1 / R2 を第一候補とする
- LINE Messaging API を利用する
- 会話状態はサーバー側で管理する
- 画像はできるだけ早く保存する
- 保守性、可読性、拡張性を重視する
- 適宜リファクタリングしてよいが、仕様は勝手に変えない

# データ方針

- 対応完了後も保持する
- 管理者が削除するまで残す

# セキュリティ方針

- 管理画面は認証必須
- Webhook署名検証を行う
- 個人情報をログに出しすぎない
- 画像を安全に保存する
- HTTPS前提
- 利用同意を取得する
- 簡易プライバシーポリシーを用意する

# Git / 作業ルール

- Claude は git commit / git push / git merge をしない
- Claude は破壊的操作をしない
- Claude は本番環境へ影響する操作をしない
- Claude は差分確認、テスト実行、コミットメッセージ案の提示までは行ってよい
- 作業は小さく分ける
- 各工程の最後で停止する
- 変更ファイル、実施内容、確認事項、次のStepを示す
- docs/progress.md を更新する

# 出力ルール

- まず目的を簡潔に述べる
- 次に実施内容を示す
- 最後に変更ファイル、確認事項、次のStepを示す
- 不明点は勝手に確定しすぎず、仮定として明示する